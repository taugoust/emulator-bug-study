[project]
name = "word-count"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
word-count = "word_count.main:main"

[tool.hatch.build.targets.wheel]
packages = ["word_count"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
