[project]
name = "analyze-diff"
version = "0.1.0"
license = "MIT"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
analyze-diff = "analyze_diff.main:main"

[tool.hatch.build.targets.wheel]
packages = ["analyze_diff"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
