[project]
name = "buglib"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["buglib"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
